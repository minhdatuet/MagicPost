import React from "react";

const Account = () => {
    return (
        <div>
            <h1 style={{textAlign: "center", margin: "auto"} }>Account</h1>
        </div>
    );
};

export default Account;